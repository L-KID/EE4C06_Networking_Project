Use `run_all.m` to get all the figures, which will be saved in the `../figures` folder.

It is also possible to manually execute the `G_simulation.m` files in the sub-folders, where `G` stands for BA, ER, or WS.  
